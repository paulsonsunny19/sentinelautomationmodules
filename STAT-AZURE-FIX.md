# STAT Azure compatibility fork

The `stat-upstream-azure-fix` branch preserves the actual upstream STAT System Assigned Managed Identity resource chain and changes package delivery to require a direct Azure Blob URL.

See `DEPLOY-STAT.md` for deployment instructions.
