# Before selecting Create

Confirm the Function Package field contains the Blob SAS URL you generated for `stat.zip`. Leave naming on default and identity on system for the first deployment. Disable the sample for the first core test. The deployment should create the Function automatically; do not pre-create a Function App named `stat-Abp` or another custom name.
