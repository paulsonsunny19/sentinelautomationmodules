# Identity

This validated compatibility path intentionally uses the upstream-recommended **System Assigned Managed Identity** mode. Service Principal and User Assigned variants can be ported after the System Assigned deployment is verified in Azure, avoiding multiple identity-path changes during initial compatibility testing.
