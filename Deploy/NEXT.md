# Next

After the System Assigned deployment is successfully validated in Azure, the upstream Service Principal and User Assigned Managed Identity templates can be added with the same direct-Blob package compatibility behavior. They are intentionally excluded from the initial test path to keep the deployment failure surface small.
