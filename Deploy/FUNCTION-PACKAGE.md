# STAT Function package

Expected package: upstream STAT-Function v2.3.0 `stat.zip`.

The package itself is not modified by this compatibility deployment. Only its hosting location changes: instead of setting `WEBSITE_RUN_FROM_PACKAGE` to a redirecting GitHub Release URL, provide a direct Azure Blob URL through `FunctionPackage`.
