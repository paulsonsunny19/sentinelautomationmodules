# Do not use the previous experimental deployment

Do not deploy the `fix/linux-consumption-package` branch for a new STAT installation. Use `stat-upstream-azure-fix`, which restores the actual upstream STAT Function-to-connector resource dependency chain.
