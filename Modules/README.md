# STAT modules

The Python STAT modules execute from the upstream STAT-Function `stat.zip` package supplied through the `FunctionPackage` deployment parameter. This repository does not alter the module implementation.
